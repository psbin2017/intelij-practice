package com.psbin2017.inflearn.intelij.autocomplete;

public class LiveTemplate {
    // 앞서본 psvm , sout 도 라이브 템플릿임
    public static void main(String[] args) {
        int score = 1;

        // ifn
        // if 구문 생성
        if (score == 0) {

        }

        // CTRL + J
        // 라인의 위치에서 선택가능한 라이브 템플릿의 내용을 보여줌
        // FIXME: 2021-01-13
    }
}
